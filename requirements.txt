# 跨平台依赖清单（Windows CUDA / macOS MPS 通用）| Python 3.13
#
# Windows (NVIDIA GPU) 先单独装 CUDA 版 torch（命中 pip 缓存，不重新下载）:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu126
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# macOS (Apple Silicon) 直接:
#   python3.13 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt

# ===== 数据处理 =====
matplotlib==3.11.1
pillow==12.3.0
PyYAML==6.0.3

# ===== 训练/推理 =====
# 纯版本号可匹配已装好的 2.14.0+cu126（PEP 440 规则），Windows 上不会重装
torch==2.14.0
torchvision==0.29.0
ultralytics==8.4.138
numpy==2.5.2
opencv-python==5.0.0.93

# ===== 大图滑窗切图推理（3648x2736 场景）=====
sahi==0.12.6
